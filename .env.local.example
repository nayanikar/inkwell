# Vite client — SpacetimeDB WebSocket connection
# Copy to .env.local (gitignored by default in many setups; not committed)

VITE_SPACETIMEDB_HOST=ws://localhost:3001
VITE_SPACETIMEDB_DB_NAME=inkwell
